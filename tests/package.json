{
  "name": "baby-monitor-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for the WebRTC Baby Monitor app",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}
